{
  "name": "chrome-qr-code",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run postbuild",
    "postbuild": "rm -f chrome-qr-code.zip && zip -r chrome-qr-code.zip dist",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.4.8",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "qrcode": "^1.5.1"
  }
}
